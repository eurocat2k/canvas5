export const Config = {
    ui: {
        // UI constants, attributes, configurable items
        ZOOM_RANGE_MIN: -2.7,       // min zoom range
        ZOOM_RANGE_MAX: 3.5,        // max zoom range
        wheelSensitivity: 0.0005,   // mousewheel sensitivity
        scaleFactor: 500,           // 1 pixel = 500 meters
        minZoom: 1,                 // minimum zoom step
        defaultZoom: 1,             // default zoom level
        maxZoom: 20,                // maximum zoom
        map_resolution: 1 / 350,    // 1 pixel = 350 meters
    }
};